import * as secret from './secret';

export const session_secret = secret.session_secret;
export const mongo_path = secret.mongo_path;
export const hash_salt = secret.hash_salt;
export const mongo_session_path = secret.mongo_session_path;
export const mongo_session_collection = secret.mongo_session_collection;

export const oauth_domain = secret.oauth_domain;
export const service_domain = secret.service_domain;
export const client_id = secret.client_id;
export const client_name = secret.client_name;
export const client_secret = secret.client_secret;

export const oauth_callback_path = secret.oauth_callback_path;
export const oauth_token_path = secret.oauth_token_path;
export const oauth_authorize_path = secret.oauth_authorize_path;
export const oauth_user_info_path = secret.oauth_user_info_path;

export const hls_root_path = secret.hls_root_path;